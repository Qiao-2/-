const STORAGE_KEY = 'novel_site_favorites';
const THEME_KEY = 'novel_site_theme';
const PROGRESS_PREFIX = 'novel_progress_';
const RECENT_KEY = 'novel_recent_books';
const FONT_SIZE_KEY = 'novel_font_size';
const LINE_HEIGHT_KEY = 'novel_line_height';
const ADMIN_KEY = 'novel_admin_unlocked';
const LIBRARY_KEY = 'novel_library_data';
const SITE_META_KEY = 'novel_site_meta';
const ADMIN_PASSWORD = '663666';
const BOOKS_URL = 'books.json';

const defaultBooks = [
  { id: 1, title: '春夜归人', author: '林知夏', category: '现代', tags: ['爱情', '都市', '完结'], summary: '一部关于重逢、成长与选择的温柔故事。', resourceUrl: 'https://example.com/your-lanzou-link-1', resourceLabel: '蓝奏云链接填写', color: 'linear-gradient(135deg, #fb7185, #a855f7)', chapters: [{ title: '第一章 旧雨初晴', content: '雨停之后，街角的咖啡店重新亮起灯。她望着窗外，仿佛一切都还来得及。\n\n有些相遇，像一封迟到的信，最终还是落进了命运的手里。\n\n她轻轻合上书页，忽然听见门铃响了。' }, { title: '第二章 风穿过站台', content: '列车缓缓进站，风吹起她的发梢。那些没说完的话，终于在这一刻有了答案。\n\n站台很长，长到足以容纳一次迟来的拥抱。' }, { title: '第三章 余温', content: '他把手里的伞递给她，像把一个安静的承诺交给了夜色。\n\n原来，最好的结局不是轰轰烈烈，而是有人愿意陪你把日子过完。' }, { title: '第四章 回声', content: '多年以后，她仍会想起那个雨天。只是这一次，心里不再只剩遗憾。' }] },
  { id: 2, title: '长安灯火', author: '沈砚', category: '古风', tags: ['历史', '权谋', '公版风格'], summary: '以古城为背景的悬疑与人情故事。', resourceUrl: 'https://example.com/your-lanzou-link-2', resourceLabel: '蓝奏云链接填写', color: 'linear-gradient(135deg, #f97316, #f43f5e)', chapters: [{ title: '第一章 夜入长安', content: '城门落下时，夜色正浓。她提着一盏灯，走进了这座传闻里最难看清真相的城。\n\n灯火一路延展，像一条沉默的河。' }, { title: '第二章 纸上春秋', content: '案卷一页页翻过，真相藏在墨迹未干的字里。每个人都说了实话，可没有一个人说完。\n\n她在字缝里看见了第二种命运。' }, { title: '第三章 风起朱雀街', content: '朱雀街的风吹灭了灯，却吹不散人们心里的执念。\n\n所有的秘密都在等一个合适的时辰被看见。' }, { title: '第四章 旧事如潮', content: '旧案重提，满城皆惊。可最惊人的，从来不是案子本身，而是人心。' }] },
  { id: 3, title: '星海邮差', author: '顾北', category: '科幻', tags: ['冒险', '未来', '连载'], summary: '在星际航路上递送信件的人，见证了无数人的告别与希望。', resourceUrl: 'https://example.com/your-lanzou-link-3', resourceLabel: '蓝奏云链接填写', color: 'linear-gradient(135deg, #38bdf8, #6366f1)', chapters: [{ title: '第一章 失重来信', content: '他收到一封没有地址的信，只写着一句话：如果你看见这行字，请替我把明天带来。\n\n信纸边缘微微卷起，像来自很远很远的地方。' }, { title: '第二章 漂浮的港口', content: '港口悬浮在云层之上，等待每一次返航。远处的星光像被打碎的玻璃，安静而锋利。\n\n每一艘船都有要抵达的终点。' }, { title: '第三章 轨道之外', content: '他终于明白，所谓邮差，不只是送信的人，更是替别人把未说出口的心意送达的人。' }, { title: '第四章 远方回信', content: '星海很大，大到足够装下所有人的离别；星海也很小，小到一封信就能把两个世界连在一起。' }] },
  { id: 4, title: '雨落图书馆', author: '南栀', category: '治愈', tags: ['成长', '校园', '短篇'], summary: '一部温暖安静的治愈向作品。', resourceUrl: 'https://example.com/your-lanzou-link-4', resourceLabel: '蓝奏云链接填写', color: 'linear-gradient(135deg, #22c55e, #14b8a6)', chapters: [{ title: '第一章 旧书架', content: '在那间总是下雨的图书馆里，时间像慢慢翻页。她把心事夹在书里，也把愿望写在了封面背面。\n\n窗外的雨声像远方的鼓点。' }, { title: '第二章 午后阳光', content: '阳光从窗格里落下，照亮了落灰的字母。她终于明白，温柔不是没有答案，而是愿意继续往前。' }, { title: '第三章 最后一页', content: '她合上书，发现自己早已不是那个躲在角落里的人。\n\n故事结束的时候，真正的生活才刚刚开始。' }, { title: '第四章 新书签', content: '她在新书上夹上书签，像给未来留下一点温柔的标记。' }] },
];

let books = defaultBooks;
const state = {
  currentBookId: defaultBooks[0].id,
  currentChapterIndex: 0,
  currentPageIndex: 0,
  filteredBooks: defaultBooks,
  favorites: JSON.parse(localStorage.getItem(STORAGE_KEY) || '[]'),
  recent: JSON.parse(localStorage.getItem(RECENT_KEY) || '[]'),
  adminUnlocked: localStorage.getItem(ADMIN_KEY) === '1',
  tagFilter: 'all',
  resourceFilter: 'all',
  selectedBookIds: [],
};

const els = {
  bookList: document.getElementById('bookList'), searchInput: document.getElementById('searchInput'), categoryFilter: document.getElementById('categoryFilter'), resourceFilter: document.getElementById('resourceFilter'), tagCloud: document.getElementById('tagCloud'), readerTitle: document.getElementById('readerTitle'), readerMeta: document.getElementById('readerMeta'), readerPanel: document.getElementById('readerPanel'), readerCategory: document.getElementById('readerCategory'), chapterSelect: document.getElementById('chapterSelect'), tocList: document.getElementById('tocList'), downloadBtn: document.getElementById('downloadBtn'), favBtn: document.getElementById('favBtn'), favoriteList: document.getElementById('favoriteList'), favoriteCount: document.getElementById('favoriteCount'), bookCount: document.getElementById('bookCount'), chapterCount: document.getElementById('chapterCount'), featuredCard: document.getElementById('featuredCard'), featuredCover: document.getElementById('featuredCover'), themeToggle: document.getElementById('themeToggle'), carouselPrev: document.getElementById('carouselPrev'), carouselNext: document.getElementById('carouselNext'), recentList: document.getElementById('recentList'), fontSizeRange: document.getElementById('fontSizeRange'), lineHeightRange: document.getElementById('lineHeightRange'), progressBar: document.getElementById('progressBar'), unlockAdminBtn: document.getElementById('unlockAdminBtn'), lockAdminBtn: document.getElementById('lockAdminBtn'), adminLocked: document.getElementById('adminLocked'), adminPanel: document.getElementById('adminPanel'), jsonEditor: document.getElementById('jsonEditor'), loadJsonBtn: document.getElementById('loadJsonBtn'), saveJsonBtn: document.getElementById('saveJsonBtn'), downloadJsonBtn: document.getElementById('downloadJsonBtn'), addBookBtn: document.getElementById('addBookBtn'), newTitle: document.getElementById('newTitle'), newAuthor: document.getElementById('newAuthor'), newCategory: document.getElementById('newCategory'), newTags: document.getElementById('newTags'), newResourceUrl: document.getElementById('newResourceUrl'), newResourceLabel: document.getElementById('newResourceLabel'), newSummary: document.getElementById('newSummary'), newColor: document.getElementById('newColor'), newChapters: document.getElementById('newChapters'), siteNameInput: document.getElementById('siteNameInput'), siteSubTitleInput: document.getElementById('siteSubTitleInput'), siteBadgeInput: document.getElementById('siteBadgeInput'), siteHeroDescInput: document.getElementById('siteHeroDescInput'), saveSiteMetaBtn: document.getElementById('saveSiteMetaBtn'), adminTitle: document.getElementById('adminTitle'), siteBadge: document.getElementById('siteBadge'), siteHeroTitle: document.getElementById('siteHeroTitle'), siteHeroDesc: document.getElementById('siteHeroDesc'), heroSideCard: document.getElementById('heroSideCard'), adminBookList: document.getElementById('adminBookList'), selectAllAdminBtn: document.getElementById('selectAllAdminBtn'), clearSelectionBtn: document.getElementById('clearSelectionBtn'), deleteSelectedBtn: document.getElementById('deleteSelectedBtn'), exportSelectedBtn: document.getElementById('exportSelectedBtn') };

function persistBooks() { localStorage.setItem(LIBRARY_KEY, JSON.stringify(books)); }
function getCurrentBook() { return books.find((b) => b.id === state.currentBookId) || books[0]; }
function getProgressKey(bookId) { return `${PROGRESS_PREFIX}${bookId}`; }
function loadProgress(bookId) { try { return JSON.parse(localStorage.getItem(getProgressKey(bookId)) || '{}'); } catch { return {}; } }
function saveProgress(bookId, data) { localStorage.setItem(getProgressKey(bookId), JSON.stringify(data)); }
function isFavorite(bookId) { return state.favorites.includes(bookId); }
function saveFavorites() { localStorage.setItem(STORAGE_KEY, JSON.stringify(state.favorites)); }
function updateStats() { els.bookCount.textContent = books.length; els.chapterCount.textContent = books.reduce((s, b) => s + b.chapters.length, 0); els.favoriteCount.textContent = state.favorites.length; }
function getAllTags() { return [...new Set(books.flatMap((book) => book.tags || []))]; }
function renderCategoryFilter() { const categories = ['all', ...new Set(books.map((b) => b.category))]; els.categoryFilter.innerHTML = categories.map((c) => `<option value="${c}">${c === 'all' ? '全部分类' : c}</option>`).join(''); }
function renderTagCloud() { const tags = getAllTags(); els.tagCloud.innerHTML = ['all', ...tags].map((tag) => `<button type="button" class="tag-pill ${state.tagFilter === tag ? 'active' : ''}" data-tag="${tag}">${tag === 'all' ? '全部标签' : tag}</button>`).join(''); els.tagCloud.querySelectorAll('.tag-pill').forEach((btn) => btn.addEventListener('click', () => { state.tagFilter = btn.dataset.tag; filterBooks(); })); }
function pushRecent(bookId) { state.recent = [bookId, ...state.recent.filter((id) => id !== bookId)].slice(0, 4); localStorage.setItem(RECENT_KEY, JSON.stringify(state.recent)); }
function renderRecent() { const list = state.recent.map((id) => books.find((b) => b.id === id)).filter(Boolean); els.recentList.innerHTML = list.length ? list.map((book) => `<div class="recent-item" data-id="${book.id}"><strong>${book.title}</strong><p>${book.author}</p></div>`).join('') : '<div class="recent-item"><p>还没有最近阅读记录。</p></div>'; els.recentList.querySelectorAll('.recent-item[data-id]').forEach((item) => item.addEventListener('click', () => { state.currentBookId = Number(item.dataset.id); const progress = loadProgress(state.currentBookId); state.currentChapterIndex = progress.chapterIndex ?? 0; state.currentPageIndex = progress.pageIndex ?? 0; renderAll(); })); }
function renderBooks(list) { els.bookList.innerHTML = list.map((book) => `<article class="library-item ${book.id === state.currentBookId ? 'active' : ''}" data-id="${book.id}"><div class="card-head"><div><span class="chip">${book.category}</span><h4>${book.title}</h4><p>${book.author}</p></div><div class="book-cover" style="background:${book.color}"></div></div><p>${book.summary}</p><div class="meta-row">${book.tags.map((tag) => `<span class="tag">${tag}</span>`).join('')}</div>${book.resourceUrl ? `<div class="meta-row"><a class="btn secondary small" href="${book.resourceUrl}" target="_blank" rel="noopener noreferrer">${book.resourceLabel || '蓝奏云链接填写'}</a></div>` : ''}</article>`).join(''); els.bookList.querySelectorAll('.library-item').forEach((item) => item.addEventListener('click', () => { state.currentBookId = Number(item.dataset.id); const progress = loadProgress(state.currentBookId); state.currentChapterIndex = progress.chapterIndex ?? 0; state.currentPageIndex = progress.pageIndex ?? 0; pushRecent(state.currentBookId); renderAll(); })); }
function loadSiteMeta() {
  try { return JSON.parse(localStorage.getItem(SITE_META_KEY) || '{}'); } catch { return {}; }
}
function saveSiteMeta(meta) {
  localStorage.setItem(SITE_META_KEY, JSON.stringify(meta));
}
function applySiteMeta() {
  const meta = loadSiteMeta();
  const title = meta.title || '番茄阅读器 Pro';
  const subtitle = meta.subtitle || '独立 `books.json` 数据源版';
  const badge = meta.badge || '仅用于你拥有授权的内容';
  const heroDesc = meta.heroDesc || '现在站点前台和后台都围绕单独的数据文件工作，你只需要维护 `books.json`，其余页面会自动读取并更新。';
  document.title = `${title} - 合法阅读与下载`;
  els.siteNameInput.value = title;
  els.siteSubTitleInput.value = subtitle;
  els.siteBadgeInput.value = badge;
  els.siteHeroDescInput.value = heroDesc;
  els.siteBadge.textContent = badge;
  els.siteHeroTitle.textContent = title;
  els.siteHeroDesc.textContent = heroDesc;
  if (els.adminTitle) els.adminTitle.textContent = `${title} 后台`;
}
function renderFeatured() { const book = getCurrentBook(); els.featuredCard.querySelector('h3').textContent = book.title; els.featuredCard.querySelector('p').textContent = book.summary; els.featuredCover.style.background = book.color; }
function renderResourcePreview() { const book = getCurrentBook(); const link = book.resourceUrl ? `<a class="btn primary" href="${book.resourceUrl}" target="_blank" rel="noopener noreferrer">${book.resourceLabel || '蓝奏云链接填写'}</a>` : '<span class="tag">暂无资源链接</span>'; const panel = document.getElementById('readerPanel'); if (!panel) return; const existing = panel.querySelector('.resource-link-row'); if (existing) existing.remove(); panel.insertAdjacentHTML('beforeend', `<div class="resource-link-row"><h4>资源链接</h4>${link}</div>`); }
function paginateText(text, maxLength = 160) { const paragraphs = text.split('\n\n'); const pages = []; let buffer = ''; paragraphs.forEach((para) => { if ((buffer + '\n\n' + para).trim().length > maxLength && buffer) { pages.push(buffer.trim()); buffer = para; } else { buffer = buffer ? `${buffer}\n\n${para}` : para; } }); if (buffer.trim()) pages.push(buffer.trim()); return pages.length ? pages : [text]; }
function persistCurrentProgress() { const book = getCurrentBook(); saveProgress(book.id, { chapterIndex: state.currentChapterIndex, pageIndex: state.currentPageIndex }); pushRecent(book.id); }
function renderReader() { const book = getCurrentBook(); const chapter = book.chapters[state.currentChapterIndex] || book.chapters[0]; const pages = paginateText(chapter.content, 165); if (state.currentPageIndex >= pages.length) state.currentPageIndex = 0; els.readerTitle.textContent = `${book.title} · 资源预览`; els.readerMeta.textContent = `${book.author} · ${book.category} · 共 ${book.chapters.length} 章`; els.readerCategory.textContent = book.category; els.chapterSelect.innerHTML = book.chapters.map((item, index) => `<option value="${index}">${item.title}</option>`).join(''); els.chapterSelect.value = String(state.currentChapterIndex); els.tocList.innerHTML = book.chapters.map((item, index) => `<div class="toc-item ${index === state.currentChapterIndex ? 'active' : ''}" data-index="${index}"><strong>${item.title}</strong></div>`).join(''); els.tocList.querySelectorAll('.toc-item').forEach((node) => node.addEventListener('click', () => { state.currentChapterIndex = Number(node.dataset.index); state.currentPageIndex = 0; persistCurrentProgress(); renderReader(); })); els.readerPanel.innerHTML = `<h4>${chapter.title}</h4>${pages[state.currentPageIndex].split('\n\n').map((p) => `<p>${p}</p>`).join('')}<div class="page-footer"><button class="btn secondary small" id="pagePrev" type="button">上一页</button><span id="pageIndicator">${state.currentPageIndex + 1} / ${pages.length}</span><button class="btn secondary small" id="pageNext" type="button">下一页</button></div><div class="resource-link-row"><h4>下载链接</h4>${book.resourceUrl ? `<a class="btn primary" href="${book.resourceUrl}" target="_blank" rel="noopener noreferrer">${book.resourceLabel || '打开资源链接'}</a>` : '<span class="tag">暂无资源链接</span>'}</div>`; document.getElementById('pagePrev').onclick = () => { state.currentPageIndex = Math.max(0, state.currentPageIndex - 1); persistCurrentProgress(); renderReader(); }; document.getElementById('pageNext').onclick = () => { state.currentPageIndex = Math.min(pages.length - 1, state.currentPageIndex + 1); persistCurrentProgress(); renderReader(); }; els.favBtn.textContent = isFavorite(book.id) ? '已收藏' : '收藏'; els.progressBar.style.width = `${Math.round(((state.currentChapterIndex + 1) / book.chapters.length) * 100)}%`; renderResourcePreview(); }
function renderFavorites() { const list = books.filter((book) => isFavorite(book.id)); els.favoriteList.innerHTML = list.length ? list.map((book) => `<div class="favorite-item" data-id="${book.id}"><strong>${book.title}</strong><p>${book.author}</p><div class="favorite-tags">${book.tags.map((tag) => `<span class="tag">${tag}</span>`).join('')}</div></div>`).join('') : '<div class="favorite-item"><p>还没有收藏任何作品。</p></div>'; els.favoriteList.querySelectorAll('.favorite-item[data-id]').forEach((item) => item.addEventListener('click', () => { state.currentBookId = Number(item.dataset.id); const progress = loadProgress(state.currentBookId); state.currentChapterIndex = progress.chapterIndex ?? 0; state.currentPageIndex = progress.pageIndex ?? 0; renderAll(); })); }
function filterBooks() { const keyword = els.searchInput.value.trim().toLowerCase(); const category = els.categoryFilter.value; const resourceFilter = els.resourceFilter.value; state.resourceFilter = resourceFilter; state.filteredBooks = books.filter((book) => { const haystack = [book.title, book.author, book.category, ...(book.tags || []), book.summary, book.resourceLabel || '', book.resourceUrl || ''].join(' ').toLowerCase(); const matchesKeyword = haystack.includes(keyword); const matchesCategory = category === 'all' || book.category === category; const hasLink = Boolean(book.resourceUrl); const matchesResource = resourceFilter === 'all' || (resourceFilter === 'with-link' && hasLink) || (resourceFilter === 'without-link' && !hasLink); const matchesTag = state.tagFilter === 'all' || (book.tags || []).includes(state.tagFilter); return matchesKeyword && matchesCategory && matchesResource && matchesTag; }); if (!state.filteredBooks.some((book) => book.id === state.currentBookId)) { state.currentBookId = state.filteredBooks[0]?.id || books[0].id; state.currentChapterIndex = 0; state.currentPageIndex = 0; } renderBooks(state.filteredBooks); renderReader(); renderFavorites(); renderFeatured(); renderTagCloud(); }
function downloadCurrentBook() { const book = getCurrentBook(); const txt = [book.title, `作者：${book.author}`, `分类：${book.category}`, '', ...book.chapters.flatMap((chapter) => [chapter.title, chapter.content, ''])].join('\n'); const blob = new Blob([txt], { type: 'text/plain;charset=UTF-8' }); const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.href = url; a.download = `${book.title}.txt`; a.click(); URL.revokeObjectURL(url); }
function toggleFavorite() { const book = getCurrentBook(); if (isFavorite(book.id)) state.favorites = state.favorites.filter((id) => id !== book.id); else state.favorites.unshift(book.id); saveFavorites(); renderAll(); }
function applyTheme(theme) { document.documentElement.dataset.theme = theme; localStorage.setItem(THEME_KEY, theme); els.themeToggle.querySelector('span').textContent = theme === 'light' ? '☀️' : '🌙'; }
function initTheme() { applyTheme(localStorage.getItem(THEME_KEY) || 'dark'); }
function applyReaderPrefs() { const fontSize = Number(localStorage.getItem(FONT_SIZE_KEY) || 16); const lineHeight = Number(localStorage.getItem(LINE_HEIGHT_KEY) || 1.95); document.documentElement.style.setProperty('--reader-font-size', `${fontSize}px`); document.documentElement.style.setProperty('--reader-line-height', String(lineHeight)); els.fontSizeRange.value = String(fontSize); els.lineHeightRange.value = String(lineHeight); }
function loadBooksFromSource() { return fetch(BOOKS_URL, { cache: 'no-store' }).then((res) => { if (!res.ok) throw new Error(`无法加载 ${BOOKS_URL}`); return res.json(); }).catch(() => defaultBooks); }
function renderAll() { updateStats(); renderCategoryFilter(); renderTagCloud(); renderBooks(state.filteredBooks); renderReader(); renderFavorites(); renderFeatured(); renderRecent(); syncAdminUI(); renderAdminBooks(); }
function openAdminPanel() { const adminSection = document.getElementById('admin'); if (adminSection) adminSection.classList.remove('hidden'); if (!state.adminUnlocked) unlockAdmin(); }
function renderAdminBooks() { if (!els.adminBookList) return; els.adminBookList.innerHTML = books.map((book) => `<div class="recent-item ${state.selectedBookIds.includes(book.id) ? 'active' : ''}" data-id="${book.id}"><strong>${book.title}</strong><p>${book.author} · ${book.category}${book.resourceUrl ? ' · 有链接' : ''}</p><div class="admin-actions"><button class="btn secondary small" data-action="edit">编辑</button><button class="btn secondary small" data-action="delete">删除</button><button class="btn secondary small" data-action="select">选择</button></div></div>`).join(''); bindAdminBookSelection(); els.adminBookList.querySelectorAll('[data-action="select"]').forEach((btn) => btn.addEventListener('click', (e) => { e.stopPropagation(); const id = Number(btn.closest('[data-id]').dataset.id); toggleSelectBook(id); })); els.adminBookList.querySelectorAll('[data-action="delete"]').forEach((btn) => btn.addEventListener('click', (e) => { e.stopPropagation(); const card = btn.closest('[data-id]'); deleteBook(Number(card.dataset.id)); })); els.adminBookList.querySelectorAll('[data-action="edit"]').forEach((btn) => btn.addEventListener('click', (e) => { e.stopPropagation(); const id = Number(btn.closest('[data-id]').dataset.id); const book = books.find((b) => b.id === id); if (!book) return; const updated = { ...book, title: prompt('书名', book.title) || book.title, author: prompt('作者', book.author) || book.author, category: prompt('分类', book.category) || book.category, summary: prompt('简介', book.summary) || book.summary, resourceUrl: prompt('蓝奏云链接', book.resourceUrl || '') || book.resourceUrl || '', resourceLabel: prompt('链接按钮文案', book.resourceLabel || '蓝奏云链接填写') || book.resourceLabel || '蓝奏云链接填写' }; books = books.map((b) => (b.id === id ? updated : b)); persistBooks(); renderAll(); syncAdminWithForm(); })); }
function toggleSelectBook(id) { if (state.selectedBookIds.includes(id)) { state.selectedBookIds = state.selectedBookIds.filter((x) => x !== id); } else { state.selectedBookIds.push(id); } renderAdminBooks(); }
function deleteSelectedBooks() { if (!state.selectedBookIds.length) return alert('请先选择要删除的作品'); if (!confirm(`确定删除 ${state.selectedBookIds.length} 条作品吗？`)) return; books = books.filter((book) => !state.selectedBookIds.includes(book.id)); state.selectedBookIds = []; persistBooks(); if (!books.length) books = defaultBooks; state.filteredBooks = books; if (!books.some((b) => b.id === state.currentBookId)) { state.currentBookId = books[0].id; state.currentChapterIndex = 0; state.currentPageIndex = 0; } renderAll(); }
function exportSelectedBooks() { const selected = books.filter((book) => state.selectedBookIds.includes(book.id)); if (!selected.length) return alert('请先选择要导出的作品'); const blob = new Blob([JSON.stringify(selected, null, 2)], { type: 'application/json;charset=UTF-8' }); const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.href = url; a.download = 'selected-books.json'; a.click(); URL.revokeObjectURL(url); }

function toggleAdminVisibility() {
  const adminSection = document.getElementById('admin');
  if (adminSection) adminSection.classList.remove('hidden');
}
function switchFeatured(step) { const currentIndex = books.findIndex((book) => book.id === state.currentBookId); const nextIndex = (currentIndex + step + books.length) % books.length; state.currentBookId = books[nextIndex].id; const progress = loadProgress(state.currentBookId); state.currentChapterIndex = progress.chapterIndex ?? 0; state.currentPageIndex = progress.pageIndex ?? 0; pushRecent(state.currentBookId); renderAll(); }
function syncAdminUI() { els.adminLocked.classList.toggle('hidden', state.adminUnlocked); els.adminPanel.classList.toggle('hidden', !state.adminUnlocked); els.jsonEditor.value = JSON.stringify(books, null, 2); if (state.adminUnlocked) applySiteMeta(); }
function unlockAdmin() { const pwd = prompt('请输入后台密码'); if (pwd === null) return; if (pwd === ADMIN_PASSWORD) { state.adminUnlocked = true; localStorage.setItem(ADMIN_KEY, '1'); syncAdminUI(); } else alert('密码错误'); }
function lockAdmin() { state.adminUnlocked = false; localStorage.removeItem(ADMIN_KEY); syncAdminUI(); }
function loadJsonIntoBooks() { try { const parsed = JSON.parse(els.jsonEditor.value); if (!Array.isArray(parsed)) throw new Error('JSON 必须是数组'); books = parsed; persistBooks(); state.filteredBooks = books; if (!books.some((b) => b.id === state.currentBookId)) { state.currentBookId = books[0]?.id || 1; state.currentChapterIndex = 0; state.currentPageIndex = 0; } renderAll(); alert('已载入并更新预览'); } catch (err) { alert(`JSON 格式错误：${err.message}`); } }
function saveJson() { try { const parsed = JSON.parse(els.jsonEditor.value); if (!Array.isArray(parsed)) throw new Error('JSON 必须是数组'); books = parsed; persistBooks(); renderAll(); alert('已保存到浏览器'); } catch (err) { alert(`JSON 格式错误：${err.message}`); } }
function syncAdminWithForm() { if (!state.adminUnlocked) return; const current = getCurrentBook(); els.newTitle.value = current.title || ''; els.newAuthor.value = current.author || ''; els.newCategory.value = current.category || ''; els.newTags.value = (current.tags || []).join(', '); els.newResourceUrl.value = current.resourceUrl || ''; els.newResourceLabel.value = current.resourceLabel || '蓝奏云下载'; els.newSummary.value = current.summary || ''; els.newColor.value = current.color || ''; els.newChapters.value = (current.chapters || []).map((chapter) => `${chapter.title}||${chapter.content}`).join('\n'); }
function saveSiteMetaFromForm() { const meta = { title: els.siteNameInput.value.trim() || '个人合法资源库', subtitle: els.siteSubTitleInput.value.trim() || '后台管理 + 搜索 + 蓝奏云直链展示', badge: els.siteBadgeInput.value.trim() || '仅用于你自己合法的资源', heroDesc: els.siteHeroDescInput.value.trim() || '本站仅展示你自己拥有版权、已授权或公版的资源，支持搜索、后台管理和合法直链展示。' }; saveSiteMeta(meta); applySiteMeta(); alert('站点信息已保存'); }
function bindAdminBookSelection() { if (!els.adminBookList) return; els.adminBookList.querySelectorAll('[data-id]').forEach((item) => { item.addEventListener('click', () => { const book = books.find((b) => b.id === Number(item.dataset.id)); if (!book) return; state.currentBookId = book.id; renderAll(); syncAdminWithForm(); }); }); }
function syncAdminWithForm() { if (!state.adminUnlocked) return; const current = getCurrentBook(); els.newTitle.value = current.title || ''; els.newAuthor.value = current.author || ''; els.newCategory.value = current.category || ''; els.newTags.value = (current.tags || []).join(', '); els.newResourceUrl.value = current.resourceUrl || ''; els.newResourceLabel.value = current.resourceLabel || '蓝奏云链接填写'; els.newSummary.value = current.summary || ''; els.newColor.value = current.color || ''; els.newChapters.value = (current.chapters || []).map((chapter) => `${chapter.title}||${chapter.content}`).join('\n'); }
function downloadJson() { const blob = new Blob([JSON.stringify(books, null, 2)], { type: 'application/json;charset=UTF-8' }); const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.href = url; a.download = 'books.json'; a.click(); URL.revokeObjectURL(url); }
function addBook() { try { const chapters = els.newChapters.value.split('\n').map((line) => line.trim()).filter(Boolean).map((line) => { const parts = line.split('||'); return { title: (parts[0] || '新章节').trim(), content: (parts[1] || '').trim() || '暂无内容' }; }); const book = { id: Date.now(), title: els.newTitle.value.trim(), author: els.newAuthor.value.trim(), category: els.newCategory.value.trim() || '未分类', tags: els.newTags.value.split(',').map((s) => s.trim()).filter(Boolean), summary: els.newSummary.value.trim(), resourceUrl: els.newResourceUrl.value.trim(), resourceLabel: els.newResourceLabel.value.trim() || '蓝奏云链接填写', color: els.newColor.value.trim() || 'linear-gradient(135deg,#fb7185,#a855f7)', chapters: chapters.length ? chapters : [{ title: '第一章', content: '暂无内容' }] }; if (!book.title || !book.author) throw new Error('书名和作者不能为空'); books.unshift(book); persistBooks(); renderAll(); syncAdminWithForm(); alert('作品已添加'); } catch (err) { alert(`添加失败：${err.message}`); } }
function deleteBook(bookId) { books = books.filter((book) => book.id !== bookId); persistBooks(); if (!books.length) books = defaultBooks; state.filteredBooks = books; if (!books.some((b) => b.id === state.currentBookId)) { state.currentBookId = books[0].id; state.currentChapterIndex = 0; state.currentPageIndex = 0; } renderAll(); syncAdminWithForm(); }
function renderAdminBooks() { if (!els.adminBookList) return; els.adminBookList.innerHTML = books.map((book) => `<div class="recent-item" data-id="${book.id}"><strong>${book.title}</strong><p>${book.author} · ${book.category}</p><div class="admin-actions"><button class="btn secondary small" data-action="edit">编辑</button><button class="btn secondary small" data-action="delete">删除</button></div></div>`).join(''); els.adminBookList.querySelectorAll('[data-action="delete"]').forEach((btn) => btn.addEventListener('click', (e) => { e.stopPropagation(); const card = btn.closest('[data-id]'); deleteBook(Number(card.dataset.id)); })); els.adminBookList.querySelectorAll('[data-action="edit"]').forEach((btn) => btn.addEventListener('click', (e) => { e.stopPropagation(); const id = Number(btn.closest('[data-id]').dataset.id); const book = books.find((b) => b.id === id); if (!book) return; const updated = { ...book, title: prompt('书名', book.title) || book.title, author: prompt('作者', book.author) || book.author, category: prompt('分类', book.category) || book.category, summary: prompt('简介', book.summary) || book.summary, resourceUrl: prompt('蓝奏云链接', book.resourceUrl || '') || book.resourceUrl || '', resourceLabel: prompt('链接按钮文案', book.resourceLabel || '蓝奏云下载') || book.resourceLabel || '蓝奏云下载' }; books = books.map((b) => (b.id === id ? updated : b)); persistBooks(); renderAll(); })); }
function persistBooks() { localStorage.setItem(LIBRARY_KEY, JSON.stringify(books)); }

els.searchInput.addEventListener('input', filterBooks);
els.categoryFilter.addEventListener('change', filterBooks);
els.resourceFilter.addEventListener('change', filterBooks);
els.downloadBtn.addEventListener('click', downloadCurrentBook);
els.favBtn.addEventListener('click', toggleFavorite);
els.themeToggle.addEventListener('click', () => applyTheme(document.documentElement.dataset.theme === 'light' ? 'dark' : 'light'));
els.carouselPrev.addEventListener('click', () => switchFeatured(-1));
els.carouselNext.addEventListener('click', () => switchFeatured(1));
els.fontSizeRange.addEventListener('input', (e) => { const size = Number(e.target.value); document.documentElement.style.setProperty('--reader-font-size', `${size}px`); localStorage.setItem(FONT_SIZE_KEY, String(size)); });
els.lineHeightRange.addEventListener('input', (e) => { const lineHeight = Number(e.target.value); document.documentElement.style.setProperty('--reader-line-height', String(lineHeight)); localStorage.setItem(LINE_HEIGHT_KEY, String(lineHeight)); });
els.unlockAdminBtn.addEventListener('click', unlockAdmin);
els.lockAdminBtn.addEventListener('click', lockAdmin);
els.loadJsonBtn.addEventListener('click', loadJsonIntoBooks);
els.saveJsonBtn.addEventListener('click', saveJson);
els.downloadJsonBtn.addEventListener('click', downloadJson);
els.addBookBtn.addEventListener('click', addBook);
els.saveSiteMetaBtn.addEventListener('click', saveSiteMetaFromForm);
els.selectAllAdminBtn.addEventListener('click', () => { state.selectedBookIds = [...books.map((book) => book.id)]; renderAdminBooks(); });
els.clearSelectionBtn.addEventListener('click', () => { state.selectedBookIds = []; renderAdminBooks(); });
els.deleteSelectedBtn.addEventListener('click', deleteSelectedBooks);
els.exportSelectedBtn.addEventListener('click', exportSelectedBooks);
window.addEventListener('keydown', (e) => {
  if (e.ctrlKey && e.altKey && (e.key === 'b' || e.key === 'B')) {
    e.preventDefault();
    openAdminPanel();
  }
});

(async () => {
  books = await loadBooksFromSource();
  if (!books.length) books = defaultBooks;
  localStorage.setItem(LIBRARY_KEY, JSON.stringify(books));
  state.filteredBooks = books;
  if (!books.some((b) => b.id === state.currentBookId)) state.currentBookId = books[0].id;
  initTheme();
  applyReaderPrefs();
  applySiteMeta();
  renderAll();
  syncAdminWithForm();
})();
